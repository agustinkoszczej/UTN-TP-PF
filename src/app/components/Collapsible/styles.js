import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    overflow: 'hidden'
  },
  measure: {
    position: 'absolute',
    opacity: 0
  }
});

export default styles;
