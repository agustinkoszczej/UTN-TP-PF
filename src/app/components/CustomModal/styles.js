import { StyleSheet } from 'react-native';
import { translucentBlack } from '@constants/colors';

const styles = StyleSheet.create({
  overlay: {
    backgroundColor: translucentBlack,
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center'
  }
});

export default styles;
