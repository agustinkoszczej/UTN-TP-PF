import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  orderContainer: {
    flex: 1,
    flexDirection: 'row'
  },
  userPicture: {
    width: 50,
    height: 50
  }
});
export default styles;
