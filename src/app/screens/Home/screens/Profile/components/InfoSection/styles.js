import { StyleSheet } from 'react-native';
import { alto, black } from '@constants/colors';

const styles = StyleSheet.create({
  tabView: {
    backgroundColor: alto
  },
  tabBar: {
    backgroundColor: black
  }
});
export default styles;
